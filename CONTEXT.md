# CONTEXT.md

> Highest-priority rules for context-compiler.
> Overrides base-skills, tsconfig, and eslint constraints.

## Best Practices Overrides

<!-- Replace standard technology patterns with project-specific ones.
     Explicitly state what is being replaced.

     Example:
     - Use `@company/forms` instead of `ReactiveFormsModule`.
       Patterns with FormBuilder, FormGroup, FormControl are not applicable.
-->


## Constraints

<!-- Hard rules with no exceptions.

     Example:
     - Never use `any`, even in tests.
     - All public service methods must have JSDoc.
-->


## Architecture Decisions

<!-- Explain WHY, not just WHAT. This helps the AI agent understand intent.

     Example:
     - Micro Frontend architecture via Module Federation.
       Shared state via EventBus only — no NgRx across MFE boundaries.
-->